# @agent-native/runtime

In-memory reference implementation of the framework-neutral AgentRuntime contract.

This package is intentionally dependency-light and is used as the behavioral reference for framework adapters and contract benchmarks.
